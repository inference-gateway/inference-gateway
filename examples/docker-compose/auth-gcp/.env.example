# Google Cloud - README.md has the commands that produce these values
GCP_SERVICE_ACCOUNT=
# Any URL you choose; tokens are minted for it and the gateway accepts only it
GATEWAY_AUDIENCE=https://inference-gateway.example.com

# Optional: a provider key to run real completions, e.g.
# OPENAI_API_KEY=
