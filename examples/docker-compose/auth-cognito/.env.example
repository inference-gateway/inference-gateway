# Amazon Cognito - README.md has the commands that produce these values
AWS_REGION=
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=
COGNITO_CLIENT_SECRET=

# Optional: a provider key to run real completions, e.g.
# OPENAI_API_KEY=
